const fs = require('fs');
let data = fs.readFileSync('./day-6/data.txt', 'utf-8').toString();

for(let i = 0; i < data.length; i++)
{
    // Do stuff
    // Day 7 is going to send me on my joker character arc...
    // Where do I even begin?
}